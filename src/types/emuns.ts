export enum RegistrationStatus {
  APPROVED = "APPROVED",
  REVIEW = "REVIEW",
  REPROVED = "REPROVED",
}

export enum DialogFrom {
  STATUS = "STATUS",
  DELETE = "DELETE",
  ADMISSION = "ADMISSION",
}
